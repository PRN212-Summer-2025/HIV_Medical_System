<Window x:Class="HIVMedicalSystem.WPFApplication.Windows.Authentication.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Width="400" Height="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F8FA">
    <Border Margin="20"
            CornerRadius="12"
            Background="White"
            Padding="30">
        <StackPanel>
            <!-- Title -->
            <TextBlock Text="🔐 Sign In" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Foreground="#2D8CFF" 
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

            <!-- Email -->
            <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="txtEmail"
                     Height="35"
                     Padding="8"
                     FontSize="14"
                     Background="#F0F3F8"
                     BorderBrush="#D0D5DD"
                     BorderThickness="1"/>

            <!-- Password -->
            <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,12,0,4"/>
            <PasswordBox x:Name="txtPassword"
                         Height="35"
                         Padding="8"
                         FontSize="14"
                         Background="#F0F3F8"
                         BorderBrush="#D0D5DD"
                         BorderThickness="1"
                         />

            <!-- Login Button -->
            <Button Content="Login"
                    Click="btnLogin_Click"
                    Height="36"
                    Margin="0,20,0,0"
                    Background="#2D8CFF"
                    Foreground="Black"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Border>
</Window>